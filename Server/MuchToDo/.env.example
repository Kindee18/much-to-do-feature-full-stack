# Deprecated placeholder
# Use the root-level .env.example as the single source of truth for configuration.
# If you need backend-only overrides, copy the root .env.example into this folder as .env and edit locally.
